import{T as e,V as t,w as n}from"./vue-Cj6fMjIV.js";import{Jr as r}from"./common-ICIeFPhK.js";var i=JSON.parse(`{"path":"/blog/cdevpgd1/","title":"Stanford-CS140 Operating System Project 随记 | 博客","lang":"zh-CN","frontmatter":{"title":"Stanford-CS140 Operating System Project 随记","createTime":"2026/03/16 19:35:54","permalink":"/blog/cdevpgd1/","tags":["OS"],"sticky":true,"description":"信息 本文为我在完成 ShanghaiTech CS130 project (Stanford CS140 project) 时的学习与思考。 关于 project Stanford CS140 Project 简单来说，本 project 提供了一个基本的 PintOS 系统，你需要根据题目对本系统进行修改、优化等，并通过每个子 project 的测...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Stanford-CS140 Operating System Project 随记\\",\\"image\\":[\\"https://image.honahec.cc/Screenshot%202026-05-22%20at%2014.40.31.png\\"],\\"dateModified\\":\\"2026-05-27T06:49:28.000Z\\",\\"author\\":[]}"],["meta",{"property":"og:url","content":"https://blog.honahec.cc/blog/cdevpgd1/"}],["meta",{"property":"og:site_name","content":"云朵角落"}],["meta",{"property":"og:title","content":"Stanford-CS140 Operating System Project 随记"}],["meta",{"property":"og:description","content":"信息 本文为我在完成 ShanghaiTech CS130 project (Stanford CS140 project) 时的学习与思考。 关于 project Stanford CS140 Project 简单来说，本 project 提供了一个基本的 PintOS 系统，你需要根据题目对本系统进行修改、优化等，并通过每个子 project 的测..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://image.honahec.cc/Screenshot%202026-05-22%20at%2014.40.31.png"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-05-27T06:49:28.000Z"}],["meta",{"property":"article:tag","content":"OS"}],["meta",{"property":"article:modified_time","content":"2026-05-27T06:49:28.000Z"}]]},"readingTime":{"minutes":27.82,"words":8347},"git":{"createdTime":1773671029000,"updatedTime":1779864568000,"contributors":[{"name":"Honahec","username":"Honahec","email":"honahec@gmail.com","commits":6,"avatar":"https://avatars.githubusercontent.com/Honahec?v=4","url":"https://github.com/Honahec"},{"name":"TalexCK","username":"TalexCK","email":"talexcks@gmail.com","commits":1,"avatar":"https://avatars.githubusercontent.com/TalexCK?v=4","url":"https://github.com/TalexCK"}],"changelog":[{"hash":"fac5f5f63b0e16f67d63332b544ef561ceaf12d7","time":1779864568000,"email":"honahec@gmail.com","author":"Honahec","message":"update"},{"hash":"d7fdcf26d4f60574aaed9aeb7047a0c402396ac7","time":1779432189000,"email":"honahec@gmail.com","author":"Honahec","message":"update"},{"hash":"d5b7c397d616fc210bbab6a6d53b832f23b0b9a4","time":1779169337000,"email":"honahec@gmail.com","author":"Honahec","message":"chore(ci): markdown lint"},{"hash":"adbc7ae0daa15236e484c41f012d104bbd77be00","time":1779118070000,"email":"honahec@gmail.com","author":"Honahec","message":"docs: sticky OS blog"},{"hash":"b4d3581e06e13f69e9980b76952662a6cb22d541","time":1779117662000,"email":"honahec@gmail.com","author":"Honahec","message":"feat: contributors"},{"hash":"ae18279887a6ca7396ed954f8e5613b3b2027485","time":1779111738000,"email":"talexcks@gmail.com","author":"TalexCK","message":"docs: update cs130 note"},{"hash":"e78db5f85f545c84b6714a81edee8b173473088a","time":1773671029000,"email":"honahec@gmail.com","author":"Honahec","message":"docs: add cs130 note"}]},"autoDesc":true,"filePathRelative":"blog/cs130.md","headers":[]}`),a={name:`cs130.md`};function o(r,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[e(`<div class="hint-container info"><p class="hint-container-title">信息</p><p>本文为我在完成 <mark>ShanghaiTech CS130 project (Stanford CS140 project)</mark> 时的学习与思考。</p></div><h2 id="关于-project" tabindex="-1"><a class="header-anchor" href="#关于-project"><span>关于 project</span></a></h2><p><a href="https://www.scs.stanford.edu/10wi-cs140/pintos/pintos_1.html#SEC1" target="_blank" rel="noopener noreferrer">Stanford CS140 Project</a></p><p>简单来说，本 project 提供了一个基本的 PintOS 系统，你需要根据题目对本系统进行修改、优化等，并通过每个子 project 的测试点。</p><p>我将其提供的 PintOS 系统进行了 docker 打包，运行在一个 Ubuntu 系统中，以使协作更顺利：<a href="https://github.com/Honahec/pintos-ubuntu-docker" target="_blank" rel="noopener noreferrer">链接</a></p><h2 id="project-1-threads" tabindex="-1"><a class="header-anchor" href="#project-1-threads"><span>project 1: Threads</span></a></h2><p><a href="https://www.scs.stanford.edu/10wi-cs140/pintos/pintos_2.html#SEC15" target="_blank" rel="noopener noreferrer">doc</a></p><p>在这个项目中，提供了一个功能及其基础的线程系统，我们需要扩展它的功能。</p><h3 id="alarm-clock" tabindex="-1"><a class="header-anchor" href="#alarm-clock"><span>Alarm Clock</span></a></h3><ul><li>问题：系统现有的 <code>timer_sleep()</code> 函数（在 <code>devices/timer.c</code>）下使用的是 busy waiting，即不断循环直至 sleep 结束。</li></ul><p>那么我们为了解决这个问题，可以考虑在线程进入睡眠时添加到一个 <code>waiting_list</code> 队列中（这个队列按照 wakeup 顺序排序），并将此线程 <code>thread_block()</code> 掉，由 <code>interrupt</code> 在每个时钟周期检测列表头是否应该苏醒，并逐个 <code>thread_unblock()</code> 直至列表头不再需要唤醒。</p><p>思路很简单，剩下的就是代码细节的问题：</p><ol><li>我们需要注意普通线程和 interrupt 线程的并发冲突问题，由于 <code>waiting_list</code> 需要在 <code>timer_sleep()</code> 函数（由普通线程调用）和 <code>timer_interrupt()</code> 函数（由 interrupt 线程调用）中同时被使用，我们需要在 <code>timer_sleep()</code> 操作该队列之前禁用 interrupt。</li><li>你会发现完成后始终有一个 <code>alarm-priority</code> 测试点 fail 掉，阅读测试点 c 代码可发现其需要优先级调度（本 project part2，我至今不理解为什么要把这个 part 排在 Priority Scheduling 前面），其对本 part 代码的影响就是在 interrupt 线程中只要唤醒了线程，就需要进行 <code>intr_yield_on_return()</code>（即将正在进行的线程扔回 <code>ready_list</code>，并按照优先级重新取出）</li></ol><p>另外，你会发现在不对提供的 PintOS 做任何修改的情况下，仍可以通过 part1 的所有测试点...</p><h3 id="priority-scheduling" tabindex="-1"><a class="header-anchor" href="#priority-scheduling"><span>Priority Scheduling</span></a></h3><ol><li><mark>Standard</mark>：实现严格的优先级调度机制。 <ul><li>当一个具有更高优先级的线程加入到 <code>ready_list</code> 中时，正在运行的低优先级线程需要 <code>thread_yield()</code> 让出 CPU 资源。</li><li>当多个线程在等待锁（lock）、信号量（semaphore）或条件变量（condition variable）时，一旦资源释放，优先级最高的等待线程应当被优先唤醒。</li></ul></li></ol><p>这个 part 比上一个复杂了一些，主要在于提出了很多模糊的术语，我们需要理解 lock, semaphore 和 condition variable 都是什么。</p><p>总体来说，我们可以将这三个名词从底层到高层排序，分别是 semaphore -&gt; lock -&gt; condition variable。</p><p>下面我们简单介绍一下这三个东西。</p><h4 id="semaphore" tabindex="-1"><a class="header-anchor" href="#semaphore"><span>semaphore</span></a></h4><p>核心是计数器+阻塞队列。</p><div class="language-c line-numbers-mode" data-highlighter="shiki" data-ext="c" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-c"><span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> semaphore </span><span style="--shiki-light:#999999;--shiki-dark:#666666;">{</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">    unsigned</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> value</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">    struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> list waiters</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span>
<span class="line"><span style="--shiki-light:#999999;--shiki-dark:#666666;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>它可以管理有限数量的资源。</p><p>我们可以使用 <code>sema_down(s)</code> 和 <code>sema_up(s)</code> 函数对资源进行获取、释放，若获取时无对应资源则阻塞直到提供资源给该线程。</p><h4 id="lock" tabindex="-1"><a class="header-anchor" href="#lock"><span>lock</span></a></h4><p>本质上是一种 <code>semaphore(1)</code>。</p><p>例如我们分配一片共享内存，多个线程可以操作这片内存，就必须对此内存进行 lock 以避免 race condition。</p><h4 id="condition-variable" tabindex="-1"><a class="header-anchor" href="#condition-variable"><span>condition variable</span></a></h4><p>多个线程等待某个条件成立后被唤醒。</p><p>好了，现在对这三个名词有了基本理解后思考一种情况：如果某个 lock 被一个低优先级线程拥有，又有一个高优先级线程进入了 <code>ready_list</code>，且希望拥有这个 lock，受 semaphore 的限制，它需要等待低优先级线程运行完释放 lock 才能被唤醒，若此时有一堆不需要该锁的中优先级线程加入，则一直不会运行原本就应该先运行的高优先级线程，这是我们不想看到的。</p><p>于是引出了该 part 的第二个任务：</p><ol start="2"><li><mark>Priority Donation</mark><ul><li>线程拥有锁后，其优先级应为持有锁的优先级（定义锁的优先级为该锁等待队列线程和持有线程的优先级最大值）。</li><li>另外，我们不难发现会出现会出现递归现象，那么整条需求链路上的线程优先级都应该被调整。</li><li>当锁被释放后，原本持有该锁的线程优先级应被更新，其可能会被 <code>thread_yield()</code>。</li></ul></li></ol><p>代码实现起来并不算难，难点在于理解上述概念（不知道我的表述是否足够清晰了）。</p><h3 id="advanced-scheduler" tabindex="-1"><a class="header-anchor" href="#advanced-scheduler"><span>Advanced Scheduler</span></a></h3><ul><li>实现一个类似于 4.4 BSD 操作系统的<mark>多级反馈队列调度器 (Multilevel Feedback Queue Scheduler, MLFQS)</mark>，以降低系统的平均响应时间。</li></ul><h4 id="什么是-bsd" tabindex="-1"><a class="header-anchor" href="#什么是-bsd"><span>什么是 BSD？</span></a></h4><p>BSD 调度器的目标是在<mark>没有外部优先级信息</mark>的情况下，自动实现公平调度和良好的交互性响应。</p><p>关键在于：</p><ol><li><p>动态优先级</p><p>不像简单的优先级调度器需要手动设置优先级，BSD 调度器会根据线程的行为自动调整优先级：</p><ul><li>CPU 密集型任务（计算多）→ 优先级降低</li><li>I/O 密集型任务（交互多）→ 优先级提高</li></ul></li><li><p>指标</p><ul><li>recent_cpu（最近 CPU 使用量）</li><li>nice 值：用户可设置（-20~20），nice 值越高 = 越&quot;友好&quot; = 优先级越低</li><li>load_avg （系统负载，影响 recent_cpu 衰减速度）</li></ul></li></ol><p>具体实现 <a href="https://www.scs.stanford.edu/10wi-cs140/pintos/pintos_7.html#SEC131" target="_blank" rel="noopener noreferrer">doc</a></p><h2 id="project-2-user-program" tabindex="-1"><a class="header-anchor" href="#project-2-user-program"><span>Project 2: User Program</span></a></h2><p><a href="https://www.scs.stanford.edu/10wi-cs140/pintos/pintos_3.html#SEC32" target="_blank" rel="noopener noreferrer">doc</a></p><p>Project 2 需要能够让程序通过 System Call 与操作系统交互，让操作系统能够处理程序的错误。</p><p>完成 Project 2 的时候需要注意可能带来影响的 bug，因为未来 Project 3 和 Project 4 都将基于 Project 2 的代码实现。</p><h3 id="process-termination-messages" tabindex="-1"><a class="header-anchor" href="#process-termination-messages"><span>Process Termination Messages</span></a></h3><p>文档要求所有用户进程退出时都打印形如 <code>name: exit(status)</code> 的信息。这里的退出并不只包括显式调用 <code>exit(status)</code>，也包括用户程序触发非法访问、非法指令等异常后被内核杀死。</p><p>因此我们在线程结构中维护 <code>exit_status</code>：</p><ul><li>正常调用 <code>SYS_EXIT</code> 时，由 <code>sys_exit()</code> 写入用户传入的 status；</li><li>用户指针非法、page fault 等异常退出时，统一通过 <code>exit_process_with_error()</code> 将状态设为 <code>-1</code>；</li><li><code>process_exit()</code> 中如果当前线程确实运行过用户进程，就打印退出信息。</li></ul><p><code>halt</code> 是一个例外。它会直接关闭 PintOS，不应该额外打印某个进程的退出信息。</p><h3 id="argument-passing" tabindex="-1"><a class="header-anchor" href="#argument-passing"><span>Argument Passing</span></a></h3><p>用户程序最终会从 <code>_start(int argc, char *argv[])</code> 开始执行，如果用户栈上没有正确放置 <code>argc</code> 和 <code>argv</code>，程序一启动就会因为访问错误栈内容而 page fault。</p><p>这里需要注意：传入 <code>process_execute()</code> 的 <code>file_name</code> 并不只是可执行文件名，而是完整命令行，例如 <code>echo x y</code>。因此我们需要先把命令行拆成两部分：</p><ol><li>第一个 token 作为真正要加载的程序名，用于 <code>thread_create()</code> 和 <code>load()</code>；</li><li>全部 token 作为参数列表，用来构造用户栈。</li></ol><p>我们在 <code>process_execute()</code> 中先复制一份命令行，然后用 <code>strtok_r()</code> 取出第一个 token 作为线程名。真正构造栈的工作放在 <code>start_process()</code> 中完成：它会再次将完整命令行拆成 <code>argv</code>，调用 <code>load(argv[0], ...)</code> 加载可执行文件，加载成功后再开始向用户栈写入参数。</p><p>栈的构造顺序需要符合 x86 的函数调用约定。由于栈向低地址增长，所以实现时大致是反着压入：</p><figure><img src="https://image.honahec.cc/Screenshot 2026-05-22 at 14.40.31.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>最终用户程序看到的栈就像普通 C 函数调用一样，<code>main(argc, argv)</code> 可以直接读取参数。这里一个很容易犯错的地方是：不能把完整命令行传给 <code>load()</code>，否则系统会尝试打开名为 <code>echo x y</code> 的文件，而不是打开 <code>echo</code>。</p><h3 id="system-calls" tabindex="-1"><a class="header-anchor" href="#system-calls"><span>System Calls</span></a></h3><p>PintOS 中，用户程序发起 system call 时，会把系统调用号和参数放在用户栈上。进入内核后，<code>syscall_handler()</code> 可以通过 <code>f-&gt;esp</code> 找到这些参数。</p><p>因此，一个 system call 的处理流程大致是：</p><ol><li>从用户栈中读取 syscall number；</li><li>根据 syscall number 判断是哪一个系统调用；</li><li>从用户栈继续读取对应参数；</li><li>验证所有用户传入的指针是否合法；</li><li>调用内核中的实际处理函数；</li></ol><h4 id="用户指针验证" tabindex="-1"><a class="header-anchor" href="#用户指针验证"><span>用户指针验证</span></a></h4><p>这部分文档中提及了两个方法，我们采用的是第二种方法：依赖页错误（page fault）机制进行检查。文档中提供的<code>get_user</code>和<code>put_user</code>函数，会尝试从用户地址读取或写入一个字节。如果访问失败，就返回错误，并让当前用户进程以 <code>-1</code> 退出。</p><p>我们基于<code>get_user</code>封装了两个函数</p><ul><li><p><code>user_read (void *kaddr, const void *uaddr, size_t size)</code></p><p>从 <code>uaddr</code> 读入 <code>size</code> 长度的内容到 <code>kaddr</code>，成功读入则返回 <code>true</code>，反之返回 <code>false</code>。</p></li><li><p><code>user_string (char *kaddr, const void *uaddr, size_t max_size)</code></p><p>从 <code>uaddr</code> 读入最多 <code>max_size</code> 长度的字符串到 <code>kaddr</code>，成功读入则返回 <code>true</code>，反之返回 <code>false</code>。</p></li></ul><p>借助这两个函数，大多数 syscall 在访问用户内存时，只需调用对应的辅助函数即可完成用户指针验证，而无需手动检查页表映射或地址合法性。</p><p>不过 <code>SYS_READ</code> 是一个特殊情况。由于该 syscall 需要向用户提供的 <code>buffer</code> 中写入数据，因此不仅要保证地址可读，还必须保证其可写。对此，我们使用 <code>put_user</code> 对 <code>buffer</code> 覆盖范围内的地址逐字节尝试写入 <code>0</code>。若写入过程中未发生异常，则说明该缓冲区可写，从而完成验证。</p><h4 id="file-syscalls" tabindex="-1"><a class="header-anchor" href="#file-syscalls"><span>File Syscalls</span></a></h4><p>这里比较重要的是文件描述符，也就是 fd。用户程序拿到的 fd 只是一个整数，而真正的文件对象是内核中的 <code>struct file *</code>。因此我们需要在每个进程中维护一个 fd table，用来把用户态的 fd 映射到内核态的文件对象。</p><p>另外，所有涉及文件系统的操作都需要注意同步问题。PintOS 提供的文件系统本身并不是完全线程安全的，如果多个进程同时读写文件，可能会出现 race condition。因此我们使用一个全局的 <code>filesys_lock</code> 来保护文件系统操作。</p><p>我们的实现中，<code>struct thread</code> 中维护了一个固定大小的 <code>fd_table[128]</code>：</p><ul><li><code>fd = 0</code> 表示标准输入，<code>read(0, ...)</code> 通过 <code>input_getc()</code> 从键盘读入；</li><li><code>fd = 1</code> 表示标准输出，<code>write(1, ...)</code> 通过 <code>putbuf()</code> 写到控制台；</li><li><code>fd &gt;= 2</code> 才对应真正打开的文件，<code>open()</code> 会从 <code>2</code> 开始寻找空位。</li></ul><p>这样做的好处是查找 fd 非常直接，用户传入的 fd 可以直接作为数组下标访问。不过它也带来了一个固定上限：每个进程最多维护 128 个 fd。对于本 project 来说这个限制足够使用。</p><p>文件相关 syscall 中还需要处理字符串指针。比如 <code>create</code>、<code>remove</code>、<code>open</code> 都会接收用户传入的文件名指针。我们不会直接把这个用户指针传入文件系统，而是先申请一页 kernel page，用 <code>user_string()</code> 把文件名复制到内核空间，再调用 <code>filesys_create()</code> / <code>filesys_open()</code> 等函数。这样即使用户传入了非法字符串，也只会终止当前用户进程，不会让文件系统拿到不可信地址。</p><p>进程退出时，还需要扫描自己的 fd table，将所有未关闭的文件逐个 <code>file_close()</code>。否则即使用户程序忘记调用 <code>close()</code>，内核也应该回收对应资源。</p><h4 id="process-wait-process-execute" tabindex="-1"><a class="header-anchor" href="#process-wait-process-execute"><span><code>process_wait</code> / <code>process_execute</code></span></a></h4><p>这两个函数在 <code>process.c</code> 中，syscall 会通过它们实现 <code>exec</code> 和 <code>wait</code>。</p><p>其中 <code>exec</code> 对应 <code>process_execute()</code>，作用是创建并启动一个新的用户进程；<code>wait</code> 对应 <code>process_wait()</code>，作用是让父进程等待某个子进程结束，并获得它的退出状态。</p><p>这部分的核心问题是：<mark>父进程和子进程之间需要共享一部分状态</mark>。</p><p>因为父进程调用 <code>exec()</code> 之后，只会得到一个子进程的 <code>tid</code>。但是仅有 <code>tid</code> 是不够的，后续还需要：</p><ul><li>知道子进程是否成功加载</li><li>知道子进程是否已经退出</li><li>知道子进程的退出状态码</li><li>等待子进程退出 / 加载</li></ul><p>所以我们需要额外维护一个 <code>child_info</code> 结构体，用来记录子进程的信息。我们创建了如下结构体</p><div class="language-c line-numbers-mode" data-highlighter="shiki" data-ext="c" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-c"><span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> child_info </span><span style="--shiki-light:#999999;--shiki-dark:#666666;">{</span></span>
<span class="line"><span style="--shiki-light:#998418;--shiki-dark:#B8A965;">  tid_t</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> tid</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">						/* 子进程tid */</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  int</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> exit_status</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">			/* 子进程退出状态码 */</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  bool</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> exited</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">					/* 子进程是否已经退出 */</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  bool</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> loaded</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">					/* 子进程是否已经加载 */</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> list_elem elem</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> semaphore wait_sema</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> semaphore load_sema</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  int</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> count</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">						/* 父/子进程有几个还在，初始值为2，count=0才会free */</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">  struct</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> lock lock</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span>
<span class="line"><span style="--shiki-light:#999999;--shiki-dark:#666666;">};</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后我们就可以基于这些信息实现 <code>exec</code> 和 <code>wait</code> 的功能。</p><ul><li><p><code>process_execute</code></p><p><code>process_execute()</code> 对应 syscall 中的 <code>exec</code>。它的返回值不只表示子进程线程是否创建成功，还需要表示子进程是否成功加载了可执行文件。</p><p>因此，父进程调用 <code>thread_create()</code> 创建子进程后不能立即返回，而是需要通过 <code>load_sema</code> 等待子进程完成加载。子进程在 <code>load()</code> 结束后，会把加载结果记录到对应的 <code>child_info</code> 中，并通过 <code>sema_up(&amp;child-&gt;load_sema)</code> 唤醒父进程。</p><p>父进程从 <code>sema_down(&amp;child-&gt;load_sema)</code> 返回后，再检查 <code>child_info-&gt;loaded</code>。如果子进程加载成功，则返回子进程的 <code>tid</code>；如果加载失败，则返回 <code>-1</code>。</p></li><li><p><code>process_wait</code></p><p><code>process_wait()</code> 对应 syscall 中的 <code>wait</code>。它需要让父进程等待指定子进程退出，并获得该子进程的退出状态。</p><p>父进程会先在自己的子进程列表中查找对应的 <code>child_info</code>。如果找不到，说明传入的 <code>tid</code> 不是当前进程的子进程，直接返回 <code>-1</code>。如果找到了对应子进程，并且子进程尚未退出，父进程就通过 <code>wait_sema</code> 阻塞等待。</p><p>子进程退出时，会把自己的退出状态写入 <code>child_info-&gt;exit_status</code>，并将 <code>child_info-&gt;exited</code> 设为 <code>true</code>，然后通过 <code>sema_up(&amp;child-&gt;wait_sema)</code> 唤醒可能正在等待它的父进程。父进程被唤醒后，就可以读取子进程的 <code>exit_status</code>，并将其作为 <code>wait</code> 的返回值。</p></li></ul><p>不过这里还需要考虑资源释放的问题。父进程和子进程的退出顺序是不确定的：可能父进程先退出，也可能子进程先退出。因此，<code>child_info</code> 不能简单地只由父进程或子进程单方面释放。</p><p>为了解决这个问题，我们使用 <code>count</code> 作为引用计数。父进程和子进程各自持有一份对 <code>child_info</code> 的引用。当其中一方不再需要这个结构体时，就调用 <code>child_release()</code> 将 <code>count</code> 减一。只有当父进程和子进程都不再需要它，也就是 <code>count</code> 变为 <code>0</code> 时，才真正释放这个 <code>child_info</code> 结构体。</p><h3 id="denying-writes-to-executables" tabindex="-1"><a class="header-anchor" href="#denying-writes-to-executables"><span>Denying Writes to Executables</span></a></h3><p>Project 2 还有一个容易忽略的要求：正在运行的可执行文件不能被写入。否则一个进程运行到一半，另一个进程把它的 executable 改掉，后续 Project 3 做 lazy loading 时会出现非常难判断的行为。</p><p>PintOS 已经提供了 <code>file_deny_write()</code> 和 <code>file_allow_write()</code>。关键在于：不能在 <code>load()</code> 结束后立刻关闭 executable 文件。因为一旦关闭，deny write 的效果也会随之消失。</p><p>因此我们的做法是在 <code>load()</code> 成功后：</p><ol><li>将打开的 executable 文件保存在当前线程的 <code>runing_file</code> 中；</li><li>调用 <code>file_deny_write()</code> 禁止其他写入；</li><li>直到该进程退出时，再在 <code>process_exit()</code> 中关闭这个文件。</li></ol><p>这样 executable 在整个进程生命周期内都会保持打开和写保护状态。</p><div class="hint-container warning"><p class="hint-container-title">警告</p><p>如果你看到这里，说不定你已经写完了你的代码，建议你执行<code>make clean &amp;&amp; make check</code> 5-10 次，防止可能出现的随机性 FAIL。</p></div><h2 id="project-3-virtual-memory" tabindex="-1"><a class="header-anchor" href="#project-3-virtual-memory"><span>Project 3: Virtual Memory</span></a></h2><p><a href="https://www.scs.stanford.edu/10wi-cs140/pintos/pintos_4.html#SEC53" target="_blank" rel="noopener noreferrer">doc</a></p><div class="hint-container warning"><p class="hint-container-title">警告</p><p>Project 3 在 Project 2 的代码基础上实现，开工前尽量保证 Project 2 的正确性，否则在 Project 3 中改 Project 2 的 bug 会非常非常痛苦。</p></div><p>Project 3 需要在 Project 2 的基础上实现一系列和内存管理相关的功能。目前用户程序运行前都要加载所有需要的页面，也没有淘汰机制，能够同时运行的程序数量和程序大小都受到物理内存大小的限制。</p><p>Project 3 并没有初始的代码，所有新增的代码文件需要前往根目录下<code>Makefile.build</code>中注册。</p><h3 id="frame-table" tabindex="-1"><a class="header-anchor" href="#frame-table"><span>Frame Table</span></a></h3><div class="hint-container warning"><p class="hint-container-title">警告</p><p>Project 3 需要处理很多地址，每次不验证<code>addr != NULL</code>都是对未来 debug 的自己不负责。</p><p>每次处理 Frame Table 时不加锁都是对未来 debug 的自己不负责。</p><p>每次发现<code>NULL</code>之后不 release 锁的更是对未来 debug 的自己不负责。</p></div><p>参考文档 4.2 一节，我们首先需要实现 Frame Table。</p><p>Frame Table 用来记录系统中已经分配出去的物理页。后续的<code>swap</code>、<code>mmap</code> 等功能都需要依赖它来查找和管理 frame，因此它是 Project 3 中虚拟内存系统的基础结构。</p><p>我们的实现中，Frame Table 使用 PintOS 提供的 <code>list.h</code> 维护。这一阶段的实现可以参考 <code>palloc.c</code> 的思路，对 frame 的获取和释放进行简单封装。需要注意的是，如果一次操作中需要获取多个 frame，而中途分配失败，就必须释放之前已经成功分配的 frame，避免产生内存泄漏。</p><p>此外，由于 Frame Table 是全局数据结构，可能被多个线程同时访问，因此对它的修改和遍历都需要使用 lock 保护，防止出现神奇又神秘的同步问题。</p><h3 id="hash-table" tabindex="-1"><a class="header-anchor" href="#hash-table"><span>Hash Table</span></a></h3><p>此处我们先介绍一下哈希表。</p><p>PintOS 在 <code>lib/kernel/hash.h</code> 和 <code>lib/kernel/hash.c</code> 中实现了一套通用的哈希表结构。它的作用和普通哈希表类似：通过一个 key 快速查找对应的元素，避免每次都在线性表中遍历查找。它只要求被存储的结构体中包含一个 <code>struct hash_elem</code> 字段。之后我们就可以通过这个 <code>hash_elem</code>，把自己的结构体挂到哈希表里。这和<code>list.h</code>是类似的。</p><p>使用哈希表时，需要提供两个函数：</p><ol><li><code>hash</code>：告诉哈希表如何根据某个字段计算哈希值；</li><li><code>less</code>：当两个元素需要比较时，告诉哈希表如何判断大小或是否相同。</li></ol><p>在实现 Supplemental Page Table 中，我们需要知道一些相关函数</p><ul><li><p><code>hash_insert()</code> 用来把一个新的元素插入哈希表中。对于 Supplemental Page Table 来说，当我们新建一个 SPT entry 后，就可以通过 <code>hash_insert()</code> 将它加入当前进程的 SPT 中。如果表中已经存在相同 <code>upage</code> 的元素，<code>hash_insert()</code> 会返回原有元素，因此也可以用它来判断是否出现了重复页面。</p></li><li><p><code>hash_find()</code> 用来在哈希表中查找元素。它并不是直接传入 key，而是传入一个临时构造的结构体元素。比如我们想通过 <code>upage</code> 查找对应的 SPT entry，就可以先创建一个只填好 <code>upage</code> 的临时 entry，再用 <code>hash_find()</code> 查找哈希表中是否存在相同页面。</p></li></ul><h3 id="supplemental-page-table" tabindex="-1"><a class="header-anchor" href="#supplemental-page-table"><span>Supplemental Page Table</span></a></h3><p>Supplemental Page Table 用来记录用户虚拟页面的额外信息，使内核能在 <code>page_fault</code> 时判断该页面是非法访问，还是需要从文件、swap 或栈增长中按需加载。</p><p>我们第二个需要实现的就是 Supplemental Page Table。这部分我们使用哈希表实现，参考<code>hash.h</code>/<code>hash.c</code>，并使用<code>upage</code>来索引。</p><p>在初始步骤中，Supplemental Page Table 结构体只需要记录<code>upage</code>和<code>hash_elem</code>即可。后续步骤中，按照需要给结构体新增项即可。</p><p>Supplemental Page Table 会贯穿整个 Project 3，无论是 Memory Mapped Files，还是 Swap，亦或是 Stack Growth、Eviction 的部分，都需要或多或少在 Supplemental Page Table 中添加点东西。</p><h3 id="lazy-loading-memory-mapped-files" tabindex="-1"><a class="header-anchor" href="#lazy-loading-memory-mapped-files"><span>Lazy Loading / Memory Mapped Files</span></a></h3><div class="hint-container warning"><p class="hint-container-title">警告</p><p>Project 3 同样需要处理很多文件，每次不给文件操作加锁，都是对未来 debug 的自己不负责。</p><p>每次给文件操作 acquire 或 release 同一个锁两遍以上的，更是对未来 debug 的自己不负责。</p></div><p>我们首先需要解释一下 Project 3 中和文件相关的页面加载到底要做什么。</p><p>在 Project 2 中，<code>process.c</code> 里的 <code>load_segment()</code> 会在程序启动时直接把可执行文件中的 segment 加载到内存里。这样显然很简单，但是非常浪费空间</p><p>Project 3 要解决的一个重要问题就是 <mark>lazy loading</mark>。具体来说，<code>load_segment()</code> 不再立刻把文件内容读入 frame，而是先为这些用户虚拟页创建 Supplemental Page Table 来记录这个页面应该如何被加载。</p><p>这样，程序启动时并不会马上占用大量物理内存。只有当用户程序真正访问某个尚未加载的页面时，才会触发 <code>page_fault</code>。此时 page fault handler 会检查 Supplemental Page Table，如果发现这个地址对应的页面是合法的、只是还没有被加载，就分配一个 frame，然后根据 SPT (Supplemental Page Table) 中记录的信息从文件中读取内容，并将这个 frame 安装到当前进程的 page table 中。</p><p>我们还需要实现<code>mmap</code>和<code>munmap</code>两个 System Call。每一次<code>mmap</code>都要对应一个单独的<code>id</code>，我们可以使用结构体<code>mmap_entry</code>来保存相关信息。同时，<code>mmap</code>的文件和<code>thread</code>绑定，我们可以把相关信息储存在<code>thread</code>中。</p><p>当用户程序调用 <code>mmap</code> 时，和 lazy loading 一样，操作系统不会立刻把整个文件读入内存，而是把这个文件映射到从 <code>addr</code> 开始的一段用户虚拟地址空间上。文件可能占据一个或多个 page，因此我们需要为这些 page 分别创建 Supplemental Page Table。</p><p>每个被映射的页面都会记录它对应的文件、文件偏移量、需要读取的字节数以及需要补零的字节数。之后当用户程序访问这些地址时，如果对应页面还没有加载，就会触发 <code>page_fault</code>，再由内核把对应的文件内容加载进 frame。</p><p>然后是<code>munmap</code>，若文件被修改，则需要写回文件系统，这里我们需要使用<code>pagedir_is_dirty (thread_current ()-&gt;pagedir, upage)</code>来判断文件是否被修改过。</p><div class="hint-container tip"><p class="hint-container-title">提示</p><p>此处可以进行一次 check，确保 Memory Mapped Files 相关测试点通过。</p></div><h3 id="stack-growth" tabindex="-1"><a class="header-anchor" href="#stack-growth"><span>Stack Growth</span></a></h3><p>Project 2 中 PintOS 只给 Stack 分配了一页的 page，这显然不够用。我们要移除这个限制。</p><p>首先我们看文档，</p><blockquote><p>Allocate additional pages only if they &quot;appear&quot; to be stack accesses.</p></blockquote><p>这里我们看到了一个非常神秘的词<code>appear</code>，Project 3 需要你给 Stack 额外的页面仅当“看起来像”栈指针，我们自然对这个出现在这很神秘的词语有疑问。不过既然文档说只要看起来像，那我们可以针对<code>page_fault</code>的地址作出判断，依据文档，我们可以给出如下判断条件</p><div class="language-c line-numbers-mode" data-highlighter="shiki" data-ext="c" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-c"><span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">bool</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> if_stack_pointer </span><span style="--shiki-light:#999999;--shiki-dark:#666666;">=</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> fault_addr </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">&gt;=</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> stack_pointer </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">-</span><span style="--shiki-light:#2F798A;--shiki-dark:#4C9A91;"> 32</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">                        &amp;&amp;</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> fault_addr </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">&lt;=</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> PHYS_BASE</span></span>
<span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">                        &amp;&amp;</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> fault_addr </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">&gt;=</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> PHYS_BASE </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">-</span><span style="--shiki-light:#2F798A;--shiki-dark:#4C9A91;"> 8</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;"> *</span><span style="--shiki-light:#2F798A;--shiki-dark:#4C9A91;"> 1024</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;"> *</span><span style="--shiki-light:#2F798A;--shiki-dark:#4C9A91;"> 1024</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于<code>page_fault</code>可能由 kernel 直接触发，也可能在 user 切换到 kernel 的时候触发，而在前者我们没有办法获取<code>f-&gt;esp</code>，所以我们需要在当前<code>thread</code>中记录其栈指针。记录的时机可以在前一次 Syscall 的时候，于是</p><div class="language-c line-numbers-mode" data-highlighter="shiki" data-ext="c" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-c"><span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">void</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;"> *</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">stack_pointer </span><span style="--shiki-light:#999999;--shiki-dark:#666666;">=</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> user </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">?</span><span style="--shiki-light:#B07D48;--shiki-dark:#BD976A;"> f</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">-&gt;</span><span style="--shiki-light:#B07D48;--shiki-dark:#BD976A;">esp</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;"> :</span><span style="--shiki-light:#59873A;--shiki-dark:#80A665;"> thread_current</span><span style="--shiki-light:#999999;--shiki-dark:#666666;"> ()</span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">-&gt;</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">esp</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>当发生<code>page_fault</code>且我们发现这是一个<code>stack_pointer</code>的时候，我们就可以分配一页 stack page。</p><div class="hint-container tip"><p class="hint-container-title">提示</p><p>此处可以进行一次 check，确保 Stack Growth 相关测试点通过。</p></div><h3 id="bitmap" tabindex="-1"><a class="header-anchor" href="#bitmap"><span>BitMap</span></a></h3><p>此处我们再简单介绍一下 Bitmap。</p><p>PintOS 在 <code>lib/kernel/bitmap.h</code> 和 <code>lib/kernel/bitmap.c</code> 中实现了一套位图结构。Bitmap 可以理解为一组连续的 bit，每一个 bit 只有两种状态：<code>true</code> 或 <code>false</code>。因此它非常适合用来记录某些资源是否已经被占用。</p><p>在 Swap Table 中，我们可以把每一个 swap slot 看作一个编号：</p><ul><li>第 <code>0</code> 位表示第 <code>0</code> 个 swap slot 是否被占用；</li><li>第 <code>1</code> 位表示第 <code>1</code> 个 swap slot 是否被占用；</li><li>以此类推。</li></ul><p>如果某一位是 <code>false</code>，说明对应的 slot 还没有被使用；如果是 <code>true</code>，说明这个 slot 已经被分配出去了。</p><p>在实现 Swap Table 时，我们需要知道一些相关函数：</p><ul><li><code>bitmap_create()</code> 用来创建一个指定大小的 bitmap。比如 swap 分区中一共有多少个 page-sized slot，就可以创建多少位的 bitmap。</li><li><code>bitmap_scan_and_flip()</code> 用来查找一段连续的空闲 bit，并将其状态翻转。对于 swap 来说，我们通常只需要找一个空闲 slot，所以可以用它找到一个空闲位置，并立刻把这个位置标记为已经使用。</li><li><code>bitmap_reset()</code> 用来把某一位重新设置为 <code>false</code>。当一个页面从 swap 中读回内存后，对应的 swap slot 就可以被释放，此时需要用 <code>bitmap_reset()</code> 把这个 slot 标记为空闲。</li><li><code>bitmap_test()</code> 用来检查某一位当前的状态。它可以判断某个 slot 是否正在被使用。</li></ul><h3 id="swap" tabindex="-1"><a class="header-anchor" href="#swap"><span>Swap</span></a></h3><div class="hint-container warning"><p class="hint-container-title">警告</p><p>在调用 Swap 的时候不检查是否成功的都是对未来 debug 的自己不负责。</p></div><p>在 Project 3 中，用户程序看到的是虚拟地址空间，但真正能放在内存里的页面数量受物理内存大小限制。当 frame 不够用时，操作系统就需要选择一个暂时不用的页面，将它从物理内存中换出。如果这个页面之后还可能被访问，就不能直接丢弃，而是需要把它保存到 swap 分区中。</p><p>因此，我们需要实现 Swap Table 来管理 swap 分区中哪些位置已经被使用，哪些位置仍然空闲。通常可以使用 bitmap 来记录 swap slot 的使用情况。</p><p>这里需要注意，Swap 的一个 slot 并非一个 page，一个 page 对应 8 个 slot。还需要注意的是 Swap 的初始化时机。我们需要在<code>init.c</code>里面初始化 Swap，我们通过<code>block_get_role (BLOCK_SWAP)</code>获得<code>BLOCK_SWAP</code>，但是太早初始化无法获得<code>BLOCK_SWAP</code>，太晚初始化会让系统跑起来了之后还没初始化 Swap，都会出事。</p><h3 id="eviction-reclamation" tabindex="-1"><a class="header-anchor" href="#eviction-reclamation"><span>Eviction &amp; Reclamation</span></a></h3><p>这里我们需要实现一套页面淘汰和页面恢复的机制。</p><p>在 Project 3 中，用户程序使用的是虚拟地址空间，但真正的物理内存大小是有限的。当没有空闲 frame 可以分配时，操作系统就不能简单地失败，而是需要从当前已经使用的 frame 中选择一个暂时不用的页面，将它从物理内存中淘汰出去，这个过程就是 eviction。</p><p>但是，被淘汰的页面不能随便直接丢弃。不同来源的页面需要用不同的方式处理，否则之后用户程序再次访问这个页面时，就无法恢复出正确内容。</p><p>因此，我们首先需要在 Supplemental Page Table 中记录页面的类型，例如：</p><div class="language-c line-numbers-mode" data-highlighter="shiki" data-ext="c" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-c"><span class="line"><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">enum</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> spt_type </span><span style="--shiki-light:#999999;--shiki-dark:#666666;">{</span></span>
<span class="line"><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">  PAGE_MMAP</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">,</span></span>
<span class="line"><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">  PAGE_EXEC</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">,</span></span>
<span class="line"><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">  PAGE_STACK</span><span style="--shiki-light:#999999;--shiki-dark:#666666;">,</span></span>
<span class="line"><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">  PAGE_OTHER</span></span>
<span class="line"><span style="--shiki-light:#999999;--shiki-dark:#666666;">};</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这样在淘汰页面时，就可以根据页面类型决定它应该被如何保存。</p><p>对于通过 <code>mmap</code> 映射的文件页面，如果页面被修改过，也就是 dirty bit 被设置过，那么在淘汰时需要把页面内容写回原文件；如果页面没有被修改，则可以直接丢弃，因为之后还可以从原文件重新读入。</p><p>对于 <code>exec</code> 相关的页面，也就是从可执行文件中 lazy loading 得到的页面，如果页面没有被修改，同样可以直接丢弃，之后 page fault 时重新从可执行文件加载即可。但是如果这个页面被修改过，就不能再简单地从原文件恢复了，因为原文件中的内容已经不是当前页面的最新内容。这种情况下就需要把页面写入 swap。</p><p>对于 stack page，它没有对应的文件作为后备存储，因此一般不能直接丢弃。即使页面当前没有被标记为 dirty，为了保证程序之后仍然能看到正确的栈内容，比较稳妥的做法也是将其写入 swap。</p><p>所以整体来说，淘汰页面时可以按照下面的思路处理：</p><ul><li><code>PAGE_MMAP</code>：dirty 时写回文件，不 dirty 时可以直接丢弃；</li><li><code>PAGE_EXEC</code>：不 dirty 时可以从原文件恢复，dirty 时需要 swap；</li><li><code>PAGE_STACK</code>：通常需要 swap；</li><li><code>PAGE_OTHER</code>：如果没有可靠的文件后备，也应该 swap。</li></ul><p>完成页面内容的保存后，还需要清除原来的 page table 映射，并更新 SPT entry 的状态。比如被换出到 swap 的页面，需要在 SPT 中记录对应的 swap slot；被直接丢弃的文件页面，则需要保留 file、offset、read bytes、zero bytes 等信息，方便之后重新加载。</p><p>接下来是如何选择要淘汰的 frame。</p><p>不是所有 frame 都适合被淘汰。例如，正在进行 I/O 的 frame 不能被淘汰，否则可能出现读写到一半页面被换走的问题。因此可以在 Frame Table 的表项中加入一个 <code>busy</code> 字段，只有当 <code>busy == false</code> 时，这个 frame 才能作为 eviction 的候选对象。比如在 syscall 读写用户 buffer、<code>mmap</code> 写回文件、swap in / swap out 等过程中，都可以临时将相关 frame 标记为 busy，避免它在关键操作中被淘汰。</p><p>在选择具体 victim frame 时，可以实现一个简化版的 clock algorithm，也就是 second-chance algorithm。</p><p>我们维护一个类似时钟指针的变量，在 Frame Table 中循环移动。每次检查当前 frame 时：</p><ol><li>如果该 frame 是 busy，则跳过；</li><li>如果该 frame 的 accessed bit 为 true，说明它最近被访问过，就把 accessed bit 清成 false，然后跳过；</li><li>如果该 frame 的 accessed bit 为 false，说明它最近没有被访问，就选择它作为 victim frame。</li></ol><p>这里用到的 accessed bit 可以通过 <code>pagedir_is_accessed()</code> 检查，并通过 <code>pagedir_set_accessed()</code> 清除。这个算法的直觉是：最近访问过的页面先给一次机会，只有连续一段时间没被访问的页面才会被淘汰。</p><p>页面被淘汰后，之后用户程序如果再次访问这个虚拟地址，就会触发 <code>page_fault</code>。这时 page fault handler 会去 Supplemental Page Table 中查找对应 entry，并根据 entry 中记录的信息恢复页面：</p><ul><li>如果页面是 file-backed 且没有被 swap，就重新从文件加载；</li><li>如果页面在 swap 中，就分配新的 frame，并从对应 swap slot 读回；</li><li>如果页面是合法的 stack growth，就创建新的 stack page；</li><li>如果 SPT 中找不到对应页面，才说明这是非法访问，需要结束进程。</li></ul><p>这些就是 Project 3 的大概的思路。到这，你可以尝试跑<strong>20</strong>次<code>make clean &amp;&amp; make check</code>，确保一切正常。</p><h2 id="project4-file-system" tabindex="-1"><a class="header-anchor" href="#project4-file-system"><span>Project4: File System</span></a></h2><p><a href="https://www.scs.stanford.edu/10wi-cs140/pintos/pintos_5.html#SEC75" target="_blank" rel="noopener noreferrer">doc</a></p><h3 id="buffer-cache" tabindex="-1"><a class="header-anchor" href="#buffer-cache"><span>Buffer cache</span></a></h3><p>目前，PintOS 访问文件系统时，总会直接从磁盘读写。</p><p>众所周知地，从磁盘访存是极慢的，<mark>我们迫切地需要一种缓存的机制提升读写效率，减少对磁盘的访问。</mark></p><p>此部分，我们需要修改文件系统，使其保留文件块的缓存。当发起读取或写入某个块的请求时，先检查该块是否在缓存中；如果在，就直接使用缓存中的数据，而不访问磁盘。否则，就从磁盘把该块取入缓存，必要时驱逐一个较旧的缓存项。在此项目中，缓存大小不得超过 64 个扇区。</p><p>由于缓存大小有限，我们需要一个淘汰算法来决定当缓存满时将哪个缓存写入磁盘空出，其复杂度应类似于 <mark>时钟算法</mark>。</p><p>可以想到，我们需要评估一个缓存是否还 “活着”，也就是说，它最近是否有被读写过？于是，类似于 Project3 的 Eviction 过程我们就是可以实现这个算法。</p><p>同时我们需要一个 <mark>write_behind</mark> 机制——内容保留在缓存中，必要时再写入磁盘。这是很好实现的，但重点是我们需要考虑到缓存机制实际降低了安全性，若文件系统崩溃，缓存的加入只会加重损失。</p><p>于是我们需要一个线程，周期性地将缓存中的内容写入磁盘，同时，不要忘记再文件系统关闭时也要将所有内容写入。可以想象，<mark>若周期过短，缓存效果将下降，若周期过长，安全性会下降</mark>，这是这个工程上必然遇到的困境。</p><p>为了加速读取，我们还可以设计一个 <mark>read_ahead</mark> 机制——当某个线程请求读取磁盘中某个块时，它有很大概率会继续读取下一个块，我们 <mark>异步</mark> 地将下一个块的内容提前写入缓存便可以加速读取。</p><div class="hint-container tip"><p class="hint-container-title">提示</p><p>设计完这些，并通过 Project2 的测试点，本部分就收工啦！</p></div>`,185)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};